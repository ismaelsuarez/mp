# Configuración PNPM
# Permitir scripts de build para dependencias críticas
enable-pre-post-scripts=true

# Aprobar automáticamente builds de paquetes seguros conocidos
# (Electron, argon2, better-sqlite3 son módulos nativos necesarios)
auto-install-peers=true

